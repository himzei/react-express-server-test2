{
  "name": "hongdangmoo",
  "version": "1.0.0",
  "description": "- client 폴더로 이동 - npm ci (node_modules 설치) - npm run build - client/build -> server/build 폴더로 이동",
  "main": "index.js",
  "scripts": {
    "build": "cd client && npm ci && npm run build && rm -rf ../server/build && mv build ../server",
    "start": "cd server npm ci && node app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
